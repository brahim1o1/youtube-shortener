/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['img.youtube.com', 'i.ytimg.com'], // Add any other YouTube domains you need
    }
  }
  
  module.exports = nextConfig